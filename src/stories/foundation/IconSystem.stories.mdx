import { Meta, IconGallery, IconItem } from "@storybook/addon-docs";

import { SpriteCategories } from "@enums";
import { Icon } from "@components/ui/Icon";

export const icons = {
  ACTIONS: ["fullscreen"],
  ARROWS: [
    "chevron-left",
    "chevron-right",
    "chevron-down",
    "chevron-first",
    "chevron-last",
  ],
  AUDIO: ["play", "pause", "shuffle", "loop", "volume"],
  COMMON: ["list", "heart", "computer"],
  MENU: ["home", "search", "layers"],
};

<Meta title="Foundation/Icon System" />

# Icon System

## Introduction

## Categories

{Object.entries(icons).map(([category, icons]) => (

<section>
    <h3><strong className={'capitalize'}>{SpriteCategories[category]}</strong> icon(s)</h3>

    {!icons.length && <p>No icons on this category!</p>}

    <IconGallery>
        {(icons ?? []).sort().map(icon => (
            <IconItem key={`${category}-${icon}`} name={icon}>
                <Icon className={'text-zinc-900'}
                      category={SpriteCategories[category]}
                      name={icon}/>
            </IconItem>
        ))}
    </IconGallery>

</section>
))}
